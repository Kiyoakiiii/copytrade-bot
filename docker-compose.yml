x-logging: &default-logging
  driver: json-file
  options:
    max-size: "50m"
    max-file: "3"

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    logging: *default-logging
    environment:
      POSTGRES_USER: ${POSTGRES_USER:?POSTGRES_USER must be set in .env}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set in .env}
      POSTGRES_DB: ${POSTGRES_DB:?POSTGRES_DB must be set in .env}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    logging: *default-logging
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data

  backend:
    build:
      context: ./backend
    restart: unless-stopped
    cpus: 1.0
    cpu_shares: 256
    mem_limit: 2g
    logging: *default-logging
    env_file: .env
    environment:
      EMBEDDED_LOW_LATENCY_WATCHER_ENABLED: "false"
      DATABASE_POOL_SIZE: "4"
      DATABASE_MAX_OVERFLOW: "2"
      HYPERLIQUID_EXECUTION_SUBACCOUNT_ADDRESSES: ${HYPERLIQUID_EXECUTION_SUBACCOUNT_ADDRESSES:-}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    command: ["sh", "-c", "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000"]
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:8000/healthz >/dev/null"]
      interval: 5s
      timeout: 2s
      retries: 12
      start_period: 5s
    expose:
      - "8000"

  watcher:
    build:
      context: ./backend
    restart: unless-stopped
    init: true
    cpu_shares: 2048
    logging: *default-logging
    env_file: .env
    environment:
      EMBEDDED_LOW_LATENCY_WATCHER_ENABLED: "false"
      DATABASE_POOL_SIZE: "12"
      DATABASE_MAX_OVERFLOW: "4"
      LOW_LATENCY_SUBMIT_DATABASE_POOL_SIZE: "8"
      LOW_LATENCY_SUBMIT_DATABASE_MAX_OVERFLOW: "4"
      WATCHER_STATUS_REFRESH_SECONDS: "5"
      HYPERLIQUID_EXECUTION_SUBACCOUNT_ADDRESSES: ${HYPERLIQUID_EXECUTION_SUBACCOUNT_ADDRESSES:-}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
      backend:
        condition: service_healthy
    command: ["python", "-m", "app.tasks.low_latency_worker"]

  watcher-subaccount:
    build:
      context: ./backend
    restart: unless-stopped
    init: true
    cpu_shares: 2048
    logging: *default-logging
    env_file: .env
    environment:
      EMBEDDED_LOW_LATENCY_WATCHER_ENABLED: "false"
      DATABASE_POOL_SIZE: "12"
      DATABASE_MAX_OVERFLOW: "4"
      LOW_LATENCY_SUBMIT_DATABASE_POOL_SIZE: "8"
      LOW_LATENCY_SUBMIT_DATABASE_MAX_OVERFLOW: "4"
      WATCHER_STATUS_REFRESH_SECONDS: "5"
      LOW_LATENCY_LEADER_ROUTE_MODE: "EXPLICIT"
      HYPERLIQUID_VAULT_ADDRESS: ""
      HYPERLIQUID_SUBACCOUNT_ADDRESS: ${HYPERLIQUID_SUBACCOUNT_ADDRESS:-}
      HYPERLIQUID_EXECUTION_SUBACCOUNT_ADDRESSES: ${HYPERLIQUID_EXECUTION_SUBACCOUNT_ADDRESSES:-}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
      backend:
        condition: service_healthy
    command: ["python", "-m", "app.tasks.low_latency_worker"]

  analytics:
    build:
      context: ./backend
    restart: unless-stopped
    cpu_shares: 128
    logging: *default-logging
    environment:
      DATABASE_URL: ${DATABASE_URL}
      DATABASE_POOL_SIZE: "2"
      DATABASE_MAX_OVERFLOW: "0"
      HYPERLIQUID_INFO_URL: ${HYPERLIQUID_INFO_URL:-https://api.hyperliquid.xyz/info}
      HYPERLIQUID_ACCOUNT_ADDRESS: ${HYPERLIQUID_ACCOUNT_ADDRESS:-}
      HYPERLIQUID_VAULT_ADDRESS: ${HYPERLIQUID_VAULT_ADDRESS:-}
      HYPERLIQUID_SUBACCOUNT_ADDRESS: ${HYPERLIQUID_SUBACCOUNT_ADDRESS:-}
      HYPERLIQUID_BACKGROUND_INFO_MIN_INTERVAL_SECONDS: "0.5"
      LEADER_PERFORMANCE_REFRESH_ENABLED: "true"
      LEADER_PERFORMANCE_REFRESH_SECONDS: "900"
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    depends_on:
      postgres:
        condition: service_healthy
    command: ["sh", "-c", "exec nice -n 19 python -m app.tasks.leader_performance_worker"]

  research:
    build:
      context: .
      dockerfile: backend/Dockerfile.research
    restart: unless-stopped
    init: true
    cpus: 0.50
    cpu_shares: 64
    mem_limit: 2g
    logging: *default-logging
    environment:
      DATABASE_URL: ${DATABASE_URL}
      DATABASE_POOL_SIZE: "1"
      DATABASE_MAX_OVERFLOW: "0"
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      RESEARCH_HISTORY_BUCKET_SECONDS: "21600"
      RESEARCH_CACHE_RETENTION_SECONDS: "172800"
      COPYTRADE_PUBLIC_INFO_MIN_INTERVAL_SECONDS: "2"
      COPYTRADE_PUBLIC_INFO_MAX_REQUESTS: "180"
      COPYTRADE_PUBLIC_INFO_RATE_STATE: /research-cache/info-api-rate-state
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - research_cache:/research-cache
    command: ["sh", "-c", "exec nice -n 19 python -m app.tasks.leader_research_worker"]

  frontend:
    build:
      context: ./frontend
    restart: unless-stopped
    cpus: 0.25
    cpu_shares: 64
    mem_limit: 512m
    logging: *default-logging
    environment:
      NEXT_PUBLIC_API_BASE: ${NEXT_PUBLIC_API_BASE:-/api}
    depends_on:
      - backend
    expose:
      - "3000"

  nginx:
    image: nginx:1.27-alpine
    restart: unless-stopped
    logging: *default-logging
    depends_on:
      - frontend
      - backend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./deploy/certs:/etc/nginx/certs:ro
      - ./deploy/htpasswd:/etc/nginx/htpasswd:ro

volumes:
  postgres_data:
  redis_data:
  research_cache:
